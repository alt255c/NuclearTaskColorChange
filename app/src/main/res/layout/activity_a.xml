<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <EditText
        android:id="@+id/etColor"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="200dp"
        android:hint="Введите цвет в HEX" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btnApplyColor"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Установить цвет"
        android:background="@drawable/blue_rounded_button"
        android:textColor="@android:color/white"
        android:layout_marginTop="16dp" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btnGenerateColor"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:background="@drawable/violet_rounded_button"
        android:textColor="@android:color/white"
        android:text="Сгенерировать цвет" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btnOpenB"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:background="@drawable/green_rounded_button"
        android:textColor="@android:color/white"
        android:text="Открыть активтость B" />

</LinearLayout>